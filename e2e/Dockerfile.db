# 真实引擎 E2E 测试镜像（DB 角色，健康版）
FROM alpine:3.20
ARG MARK=base
ARG VERSION=0.0.0
LABEL org.opencontainers.image.version=$VERSION
RUN mkdir -p /opt && echo "$MARK" > /opt/mark
HEALTHCHECK --interval=2s --timeout=1s --start-period=1s --retries=2 CMD /bin/sh -c "exit 0"
CMD ["sleep", "3600"]
